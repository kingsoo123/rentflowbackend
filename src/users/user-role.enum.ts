/** Values match `rent_pilot` signup select (`AuthOnboarding.tsx`). `admin` is platform-only (not self-serve). */
export enum UserRole {
  PROPERTY_MANAGER = 'property_manager',
  TENANT = 'tenant',
  ARTISAN = 'artisan',
  ADMIN = 'admin',
}
